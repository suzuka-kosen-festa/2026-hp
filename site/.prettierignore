dist/
.astro/
node_modules/

# Markdown は整形しない。
# CLAUDE.md や docs/ は「なぜそうしたか」を日本語で長く書いてあり、Prettier に
# 再折り返しさせると1文字直しただけで段落全体が差分になり、レビューで読めなくなる。
*.md

# 生成物・外部由来のファイル
public/
src/assets/

# prettier-plugin-astro がこのファイルを壊す（Issue #52 で発覚）。
# <title>{site.eventName}「{site.theme}」</title> のように式と地の文が混ざった
# 要素を扱えず、</head> を title の途中で閉じ、OGPのmetaを head の外に出し、
# 2回目の整形では <body> と <main> ごと消す。
# 本番は holding モードで全URLがこのページを返すため、壊れると全滅する。
src/pages/holding.astro
